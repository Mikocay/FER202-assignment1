import React from 'react';
import { Box, Typography } from '@mui/material';

const Footer = () => {
    return (
        <Box component="footer" sx={{ mt: 'auto', p: 2, backgroundColor: '#f5f5f5' }}>
            <Typography align='center'>2024 Courses</Typography>
        </Box>
    );
};

export default Footer;